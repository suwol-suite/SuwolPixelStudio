# Exporters

Declare an exporter contribution and `file.export`. The host provides a bounded read-only document snapshot and options. Return one or more relative-path byte entries. Absolute paths, traversal, duplicate paths, excessive counts, unsafe extensions, and output-budget overflow are rejected before an opaque save operation.
